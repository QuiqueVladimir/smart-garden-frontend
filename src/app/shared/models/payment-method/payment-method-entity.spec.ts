import { PaymentMethodEntity } from './payment-method-entity';

describe('PaymentMethodEntity', () => {
  it('should create an instance', () => {
    expect(new PaymentMethodEntity()).toBeTruthy();
  });
});
