import { PurchasedCourseEntity } from './purchased-course-entity';

describe('PurchasedCourseEntity', () => {
  it('should create an instance', () => {
    expect(new PurchasedCourseEntity()).toBeTruthy();
  });
});
